<Window x:Class="Licorp_CombineCAD.Views.SaveSheetSetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save Sheet Set"
        Height="240" Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        Foreground="White">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Save selected sheets as Revit Sheet Set"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <TextBox Grid.Row="1"
                 x:Name="SetNameTextBox"
                 Text="{Binding SetName, UpdateSourceTrigger=PropertyChanged}"
                 Background="#2D2D2D"
                 Foreground="White"
                 BorderBrush="#3E3E3E"
                 Padding="8,6"
                 MinHeight="32"/>

        <TextBlock Grid.Row="2"
                   Text="{Binding InfoText}"
                   Foreground="#A0A0A0"
                   TextWrapping="Wrap"
                   Margin="0,10,0,0"/>

        <TextBlock Grid.Row="3"
                   Text="{Binding ErrorMessage}"
                   Foreground="#FFB900"
                   TextWrapping="Wrap"
                   Margin="0,8,0,0"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Save" Width="82" Height="30" Margin="0,0,8,0" Click="Save_Click"/>
            <Button Content="Cancel" Width="82" Height="30" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
